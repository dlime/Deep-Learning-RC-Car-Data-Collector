Introduction:
	Run cali_server.py to car calibration server routine (calibration is stored in "server/config")
	Run tcp_server.py for car control GUI server routine.

Notice:
	Before you run the client routine, you must first run the server routine:
	* cali_server.py for cali_client.py
	* tcp_server.py for client_App.py